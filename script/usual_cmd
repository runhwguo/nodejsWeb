# 生成两级tree
tree -L 2 > test.tree

# pm2 start 指定运行环境
pm2 start app.json --env test

# create database
create database schoolResourceShare;

# npm install dev版本  npm 5.0.0以上自动save到package.json
cnpm i * -D